<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!--
      Forge Knowledge System — Phase 2, the semantic layer.

      A standalone Maven project (no parent, NOT part of the kernel reactor). It turns the frozen
      kernel's primitives into AI-engineering knowledge, consuming ONLY the public kernel API
      (kernel-core / kernel-api 1.0.0) resolved from the local repository. No kernel internal is used
      and no kernel change is required (Phase 2 rule). Applications (Phase 3+) depend on this module.
    -->

    <groupId>com.broksforge.knowledge</groupId>
    <artifactId>forge-knowledge</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>Forge Knowledge System</name>
    <description>The canonical ontology of AI Engineering as a semantic layer over the Forge Kernel.</description>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <kernel.version>1.0.0</kernel.version>
        <junit.version>5.11.4</junit.version>
        <maven-compiler-plugin.version>3.13.0</maven-compiler-plugin.version>
        <maven-surefire-plugin.version>3.5.4</maven-surefire-plugin.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.broksforge.kernel</groupId>
            <artifactId>kernel-core</artifactId>
            <version>${kernel.version}</version>
        </dependency>
        <dependency>
            <groupId>com.broksforge.kernel</groupId>
            <artifactId>kernel-api</artifactId>
            <version>${kernel.version}</version>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <plugin><groupId>org.apache.maven.plugins</groupId><artifactId>maven-resources-plugin</artifactId><version>3.3.1</version></plugin>
                <plugin><groupId>org.apache.maven.plugins</groupId><artifactId>maven-jar-plugin</artifactId><version>3.4.2</version></plugin>
                <plugin><groupId>org.apache.maven.plugins</groupId><artifactId>maven-clean-plugin</artifactId><version>3.4.1</version></plugin>
                <plugin><groupId>org.apache.maven.plugins</groupId><artifactId>maven-install-plugin</artifactId><version>3.1.4</version></plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven-compiler-plugin.version}</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                    <compilerArgs><arg>-Xlint:all</arg></compilerArgs>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${maven-surefire-plugin.version}</version>
            </plugin>
        </plugins>
    </build>
</project>
